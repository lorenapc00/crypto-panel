{
  "name": "crypto-panel",
  "private": true,
  "version": "0.1.0",
  "workspaces": ["apps/*", "packages/*"],
  "scripts": { "dev": "npm run dev --workspace @crypto-panel/web", "build": "npm run build --workspaces", "test": "npm run test --workspace @crypto-panel/api" }
}
